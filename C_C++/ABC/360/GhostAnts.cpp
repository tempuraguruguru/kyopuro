#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main(){
    int N, T; cin >> N >> T;
    string S; cin >> S;
    vector<int> X(N);
    for(int i = 0; i < N; i++) cin >> X[i];

    for(int i = 0; i < N; i++){
    }
}