#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

int main(){
    int N, M;
    cin >> N >> M;
    vector<ll> A(N);
    for(int i = 0; i < N; i++) cin >> A[i];

    vector<vector<ll>> res;
    for(int i = 0; i < N; i++){
        for(int j = 0; j < N; j++){
            for(int k = 0; k <= i; k++){

            }
        }
    }
}