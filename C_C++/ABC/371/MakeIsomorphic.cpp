#include <iostream>
#include <vector>
using namespace std;

int main(){
    int N, Mg, Mh;
    cin >> N >> Mg;
    vector<int> U(Mg), V(Mg);
    for(int i = 0; i < Mg; i++) cin >> U[i] >> V[i];
    cin >> Mh;
    vector<int> A(Mh), B(Mh);
    for(int i = 0; i < Mh; i++) cin >> A[i] >> B[i];


}